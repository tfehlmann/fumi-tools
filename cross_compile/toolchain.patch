--- /opt/osxcross/target/toolchain.cmake2019-11-05 14:04:35.000000000 +00000000000
+++ toolchain.cmake2020-01-17 17:46:38.576844554 +0000
@@ -12,13 +132,19 @@
   endif()
 endmacro()
 
-osxcross_getconf(OSXCROSS_HOST)
-osxcross_getconf(OSXCROSS_TARGET_DIR)
-osxcross_getconf(OSXCROSS_TARGET)
-osxcross_getconf(OSXCROSS_SDK)
+#osxcross_getconf(OSXCROSS_HOST)
+#osxcross_getconf(OSXCROSS_TARGET_DIR)
+#osxcross_getconf(OSXCROSS_TARGET)
+#osxcross_getconf(OSXCROSS_SDK)
+
+set(OSXCROSS_HOST x86_64-apple-darwin18)
+set(OSXCROSS_TARGET_DIR /opt/osxcross/target)
+set(OSXCROSS_TARGET darwin18)
+set(OSXCROSS_SDK /opt/osxcross/target/SDK/MacOSX10.14.sdk)
 
 set(CMAKE_SYSTEM_NAME "Darwin")
 string(REGEX REPLACE "-.*" "" CMAKE_SYSTEM_PROCESSOR "${OSXCROSS_HOST}")
+set(APPLE TRUE)
 
 # specify the cross compiler
 if(CMAKE_SYSTEM_PROCESSOR MATCHES "^i.86$")
